# Basic logger template for python

## Usage
First import the module: `from logger import Logger`

Then when you need logging: 
```python
log = loggerLogger(__name__)
log.debug('Debug message')
log.critical('Critical error message')
```
